#include <stdio.h>

/**
  * main - The entry point for the program
  * Return: The exit code
  */
int main(void)
{
int low, newline;

for (low = 97; low <= 122; low++){

if (low ==  113 || low == 101)
continue;
}

newline = 10;

putchar((char) low);

putchar((char) newline);


return 0;
}

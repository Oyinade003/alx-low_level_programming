#include <stdio.h>

/**
 * main - Entry point
 * author: sammy
 * Return: Always 0 (Success)
 */
int main(void)
{
int oyin, newline;
newline = 10;

for (oyin = 48; oyin < 58; oyin++)
putchar((char) oyin);
putchar((char) newline);
return (0);
}
